import React from 'react';

const SearchBar = props => {
    return (
        <form className='search'>
            <input
             type='text'
             placeholder='search'
             />
        </form>
    );
};

export default SearchBar; 