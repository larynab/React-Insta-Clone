import React from 'react';

const SearchBar = props => {
    return (
        <form>
            <input
             type='text'
             placeholder='search'
             />
        </form>
    );
};

export default SearchBar; 